version: '3'

services:
  postgres:
    image: postgres:16
    environment:
      - 'POSTGRES_DB=etutor_xquery'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=etutor_xquery'
    ports:
      - '5434:5432'
